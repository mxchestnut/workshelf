# Core Framework
fastapi==0.124.4
uvicorn[standard]==0.38.0
pydantic==2.12.5
pydantic-settings==2.12.0
email-validator==2.3.0

# Database
sqlalchemy[asyncio]==2.0.45
asyncpg==0.30.0
psycopg2-binary  # For Alembic migrations (version flexible for ARM64)
alembic==1.14.1

# Caching & Rate Limiting
redis==5.2.1

# Authentication & Security
python-jose[cryptography]==3.4.0
cryptography>=41.0.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.18
# Security: pin ecdsa to patched version (python-jose dependency)
ecdsa>=0.19.0
# Microsoft Entra ID (Azure AD) authentication
msal==1.31.1
PyJWT==2.8.0

# Payment Processing
stripe==14.0.1

# Price Scraping & Web Requests
aiohttp==3.13.2
beautifulsoup4==4.12.2

# Matrix Protocol
matrix-nio==0.24.0
lxml>=5.0.0  # Python 3.13 compatible

# AI Services
anthropic==0.75.0  # Claude API for creative writing assistance

# EPUB Processing
ebooklib==0.18
html2text==2020.1.16

# Document Format Support
python-docx==1.1.2  # Microsoft Word (.docx)
odfpy==1.4.1  # OpenDocument (.odt)
PyPDF2==3.0.1  # PDF text extraction
markdown2==2.5.1  # Enhanced markdown parsing

# Azure SDK
azure-storage-blob==12.19.0
azure-identity==1.16.1

# AWS SDK
boto3==1.35.0  # For SES email sending

# Error Tracking
sentry-sdk[fastapi]==2.18.0

# Metrics & Observability
prometheus-fastapi-instrumentator==7.0.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.27.2

# Development
python-dotenv==1.0.1
requests==2.32.4  # For Keycloak setup script
# Security: pin urllib3 to patched version (requests/boto3 dependency)
urllib3>=2.5.0
