name: Deploy to Production

on:
  workflow_dispatch:  # Manual trigger only
    inputs:
      confirm:
        description: 'Type "deploy" to confirm'
        required: true
      skip_tests:
        description: 'Skip tests (not recommended)'
        type: boolean
        default: false

jobs:
  # Gate: ensure CI passed on this commit
  verify-ci:
    name: Verify CI Status
    runs-on: ubuntu-latest
    if: github.event.inputs.confirm == 'deploy'
    
    steps:
      - name: Check CI status
        if: ${{ !inputs.skip_tests }}
        uses: actions/github-script@v7
        with:
          script: |
            const runs = await github.rest.actions.listWorkflowRunsForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'ci.yml',
              head_sha: context.sha,
              status: 'completed',
            });
            
            const ciRun = runs.data.workflow_runs[0];
            if (!ciRun || ciRun.conclusion !== 'success') {
              core.setFailed('CI workflow must pass before deploying');
            }

  deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest
    needs: verify-ci
    if: github.event.inputs.confirm == 'deploy'
    environment:
      name: production
      url: https://workshelf.dev
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup SSH
        env:
          # actionlint: ignore
          SSH_PRIVATE_KEY: ${{ secrets.EC2_SSH_KEY }}
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/workshelf-key.pem
          chmod 600 ~/.ssh/workshelf-key.pem
          # actionlint: ignore
          ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts
      
      - name: Deploy to EC2
        env:
          # actionlint: ignore
          EC2_HOST: ${{ secrets.EC2_HOST }}
          EC2_USER: ubuntu
          GIT_SHA: ${{ github.sha }}
        run: |
          ssh -i ~/.ssh/workshelf-key.pem $EC2_USER@$EC2_HOST << 'DEPLOY'
            set -e
            cd /opt/workshelf
            
            echo "🔧 Fixing git permissions..."
            sudo chown -R ubuntu:ubuntu /opt/workshelf/.git
            
            echo "📥 Pulling latest code..."
            git fetch origin main
            git reset --hard origin/main
            
            echo "🔧 Setting release tracking..."
            echo "GIT_SHA=${{ github.sha }}" >> deploy/.env.tmp
            grep -v "^GIT_SHA=" deploy/.env > deploy/.env.old || true
            cat deploy/.env.old deploy/.env.tmp > deploy/.env
            rm deploy/.env.tmp deploy/.env.old
            
            echo "🏗️ Rebuilding services..."
            cd deploy
            sudo docker-compose -f docker-compose.prod.yml build --no-cache backend frontend
            
            echo "♻️  Restarting all services..."
            sudo docker-compose -f docker-compose.prod.yml down
            
            # Clean up ALL docker resources
            echo "🧹 Cleaning up Docker..."
            sudo docker ps -a --format '{{.Names}}' | grep -E '(deploy-|workshelf-)' | xargs -r sudo docker rm -f || true
            sudo docker container prune -f
            sudo docker network prune -f
            
            # Check what's using the ports
            echo "🔍 Checking ports..."
            sudo ss -tlnp | grep -E ':(8000|5173|9000|9001)' || echo "No processes found on target ports"
            
            # Kill any docker-proxy processes that might be stuck
            sudo pkill -9 docker-proxy || true
            sleep 10
            
            # Start services one at a time to avoid race conditions
            echo "🚀 Starting infrastructure services..."
            sudo docker-compose -f docker-compose.prod.yml up -d postgres redis minio keycloak
            sleep 15
            
            echo "🚀 Starting application services..."
            sudo docker-compose -f docker-compose.prod.yml up -d backend frontend
            
            echo "🔧 Fixing CORS configuration..."
            sudo bash fix-cors.sh || echo "⚠️  CORS fix script not available yet"
            
            echo "⏳ Waiting for services to stabilize..."
            sleep 30
            
            echo "🏥 Health checks..."
            curl -f https://api.workshelf.dev/health || exit 1
            curl -f https://workshelf.dev/health || exit 1
            
            echo "✅ Deployment complete!"
          DEPLOY
      
      - name: Notify Sentry of release
        env:
          # actionlint: ignore
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          # actionlint: ignore
          SENTRY_ORG: ${{ secrets.SENTRY_ORG }}
        run: |
          curl -sL https://sentry.io/get-cli/ | bash
          export SENTRY_RELEASE="${{ github.sha }}"
          sentry-cli releases new "$SENTRY_RELEASE" || true
          sentry-cli releases set-commits "$SENTRY_RELEASE" --auto || true
          sentry-cli releases finalize "$SENTRY_RELEASE" || true
          sentry-cli releases deploys "$SENTRY_RELEASE" new -e production || true
